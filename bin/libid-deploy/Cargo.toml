[package]
name = "libid-deploy"
version = "0.1.0"
edition = "2021"
rust-version = "1.94.1"
license = "MIT OR Apache-2.0"
repository = "https://github.com/libid-org/chain-configurations"
description = "Applies per-network desired-state configuration to chains: deploys, upgrades and configures the libid contract stack, then writes the deployed addresses back into the network file."
publish = false

[dependencies]
libid-contracts = "0.2.0"
alloy = { version = "1", features = [
    "contract",
    "providers",
    "network",
    "transport-http",
    "serde",
    "signers",
    "signer-local",
    "signer-aws",
] }
# KMS-backed signing: `signer-aws` re-exports `alloy::signers::aws::AwsSigner`,
# but constructing one needs an aws_sdk_kms::Client, so the SDK crates are
# direct dependencies pinned to the 1.x line cargo unifies with alloy's.
aws-config = "1"
aws-sdk-kms = "1"
anyhow = "1"
clap = { version = "4", features = ["derive"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
toml = "0.8"
# `apply` rewrites the network file in place; toml_edit preserves the
# comments and formatting a plain serializer would destroy.
toml_edit = "0.22"
tokio = { version = "1", features = ["macros", "rt-multi-thread", "time"] }
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
url = "2"

[dev-dependencies]
alloy = { version = "1", features = ["node-bindings"] }
tempfile = "3"
